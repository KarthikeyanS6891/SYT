services:
  backend:
    environment:
      CORS_ORIGINS: http://localhost,http://localhost:80,http://localhost:5173,http://localhost:5050
      PUBLIC_URL: http://localhost:5050
    ports: !override
      - "5050:5000"

  frontend:
    build:
      args:
        VITE_API_BASE: http://localhost:5050/api/v1
        VITE_SOCKET_URL: http://localhost:5050
